package app.pulse.core.ui

enum class ColorSource {
    Default,
    Dynamic,
    MaterialYou,
    Pink
}

enum class ColorMode {
    System,
    Light,
    Dark
}

enum class Darkness {
    Normal,
    AMOLED,
    PureBlack
}
